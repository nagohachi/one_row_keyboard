[project]
name = "one-row-keyboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=3.6.0",
    "pandas>=2.3.0",
    "tqdm>=4.67.1",
]
